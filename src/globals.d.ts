/** Injected by esbuild `define` from package.json (single-sourced version). */
declare const __SERVER_VERSION__: string;

/**
 * Injected by esbuild `define` from scripts/skills.mjs — the validated
 * SEP-2640 skill catalog, JSON-encoded. Empty when the build found no skills.
 */
declare const __SKILLS_SNAPSHOT__: string;
