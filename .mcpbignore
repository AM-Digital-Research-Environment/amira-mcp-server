# The esbuild bundles (server/) are fully self-contained — verified to run
# with node_modules absent — so the packed extension ships only the bundles +
# the JSON snapshot. No node_modules, no native bindings, cross-platform.
node_modules/

# Source & build config
src/
scripts/
tsconfig.json
*.ts
*.map

# Tests
test/
smoke-test.mjs
smoke-http-test.mjs

# Local caches
.census-cache/

# Dev metadata
.gitignore
.gitattributes
.agents/
.git/
.github/
.DS_Store
*.log
.env
.env.*

# Keep in the bundle: manifest.json, package.json, icon.png, server/, data/, README.md, ROADMAP.md, .claude/ (companion skill)
